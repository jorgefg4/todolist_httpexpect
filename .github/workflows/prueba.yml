name: Test 

on:
  push:
    branches: [ actions ]

  workflow_dispatch:

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Estamos ejecutando una prueba
      run: go test ./...